// var nConfigs = 0;
// var nMyConfigs = 0;
// var allFaceIndexes = {};
// var currentConfig = null;
// var isEdit = false;

// FRONTEND STUFF

// var newParameters;

// function createNewConfig() {
//   if (uid != null) {
//     var dir = 'users/' + (uid);
//     var dbRef = firebase.database().ref(dir + '/faces/' + (nMyConfigs) + '/');
//     dbRef.set(newParameters);
//     dbRef = firebase.database().ref(dir);
//     dbRef.update({currentFace:nMyConfigs});
//   }
// }

